# 模块规划与边界

## 1. 文档目标

定义仓库结构、后端模块、前端模块、组件化规则、依赖方向与大文件治理标准。

## 2. Workspace 结构

当前主结构已冻结为：

- `services/`：二进制入口
- `crates/`：后端分层能力
- `apps/`：Admin 与 Portal 产品
- `console/`：兼容与过渡外壳
- `bin/`：安装、构建、启动、停止、服务注册入口
- `scripts/`：开发辅助与专项脚本
- `docs/`：文档与参考

## 3. 后端模块规划

### 3.1 基础设施与平台底座

- `sdkwork-api-config`
- `sdkwork-api-kernel`
- `sdkwork-api-observability`
- `sdkwork-api-cache-*`
- `sdkwork-api-secret-*`
- `sdkwork-api-policy-*`

### 3.2 契约层

- `sdkwork-api-contract-openai`
- `sdkwork-api-contract-gateway`
- `sdkwork-api-openapi`

### 3.3 领域层

- `sdkwork-api-domain-routing`
- `sdkwork-api-domain-identity`
- `sdkwork-api-domain-usage`
- `sdkwork-api-domain-billing`
- `sdkwork-api-domain-catalog`
- `sdkwork-api-domain-tenant`
- `sdkwork-api-domain-rate-limit`
- `sdkwork-api-domain-commerce`
- `sdkwork-api-domain-marketing`
- `sdkwork-api-domain-jobs`
- `sdkwork-api-domain-credential`

### 3.4 应用层

- `sdkwork-api-app-gateway`
- `sdkwork-api-app-routing`
- `sdkwork-api-app-identity`
- `sdkwork-api-app-usage`
- `sdkwork-api-app-billing`
- `sdkwork-api-app-catalog`
- `sdkwork-api-app-tenant`
- `sdkwork-api-app-rate-limit`
- `sdkwork-api-app-commerce`
- `sdkwork-api-app-marketing`
- `sdkwork-api-app-jobs`
- `sdkwork-api-app-credential`
- `sdkwork-api-app-extension`
- `sdkwork-api-app-runtime`

### 3.5 存储与执行适配层

- 存储：`sdkwork-api-storage-core/sqlite/postgres/libsql/mysql`
- Provider：`sdkwork-api-provider-core/openai/openrouter/ollama`
- 扩展：`sdkwork-api-extension-core/abi/host`、`sdkwork-api-ext-provider-native-mock`
- 运行时：`sdkwork-api-runtime-host`、`sdkwork-api-product-runtime`
- 接口：`sdkwork-api-interface-http/admin/portal`

### 3.6 商业化演进约束

- 商业化 canonical 架构以 `API Product + Commercial Account + Coupon-First Marketing` 为主线，详见 [166-API产品-商业账户-coupon-first营销统一架构-2026-04-10](./166-API产品-商业账户-coupon-first营销统一架构-2026-04-10.md)。
- `coupon` 保持一等业务语义，归属于 `marketing` 子域，不允许再被抽象回难以维护的 generic code / template 语义。
- legacy coupon runtime 与 `/admin/coupons` 控制面已移除；`marketing` 是 coupon 语义与治理的唯一真值来源。

## 4. 服务边界

- `gateway-service`：只承担数据平面与兼容入口，不承担后台治理
- `admin-api-service`：只承担治理、配置、运营、诊断，不承担热路径推理
- `portal-api-service`：只承担客户自助与账户能力，不直连 Provider 执行细节
- `router-web-service`：只承担静态站点、公网入口与反向代理，不承载复杂业务规则
- `router-product-service`：只做产品装配与统一交付，不吞并内部边界

## 5. 前端模块规划

### 5.1 Admin

基础包：

- `sdkwork-router-admin-admin-api`
- `sdkwork-router-admin-auth`
- `sdkwork-router-admin-shell`
- `sdkwork-router-admin-core`
- `sdkwork-router-admin-commons`
- `sdkwork-router-admin-types`

业务包：

- `overview`
- `users`
- `tenants`
- `apirouter`
- `catalog`
- `traffic`
- `operations`
- `settings`
- `pricing`
- `commercial`
- `coupons`

### 5.2 Portal

基础包：

- `sdkwork-router-portal-portal-api`
- `sdkwork-router-portal-auth`
- `sdkwork-router-portal-core`
- `sdkwork-router-portal-commons`
- `sdkwork-router-portal-types`
- `sdkwork-router-portal-user`

业务包：

- `home`
- `gateway`
- `dashboard`
- `api-keys`
- `usage`
- `billing`
- `credits`
- `recharge`
- `settlements`
- `routing`
- `models`
- `docs`
- `api-reference`
- `downloads`
- `commerce`
- `account`
- `console`

### 5.3 Console

`console/` 继续保留，但其职责只限于兼容、落地页与过渡外壳，不再作为主线控制平面与门户的权威承载。

## 6. 组件化标准

- 页面容器负责装配，不直接写复杂业务规则
- API client、状态管理、展示组件必须分离
- 公共表格、过滤器、详情面板、状态标签、表单片段必须复用
- 业务模块按“一个工作区一个主职责”拆分，不把所有页面堆到同一包
- 网关路由、管理接口、门户接口都必须围绕能力家族拆分，而不是堆积成巨型入口文件

## 7. 依赖方向与治理规则

- `contract` 不依赖 `app`
- `domain` 不依赖 `interface`
- `app` 依赖 `domain` 与抽象契约
- `interface` 依赖 `app`
- `storage/provider/runtime` 依赖 `domain` 与契约，但不反向支配 `interface`
- `services` 只做装配

大文件治理规则：

- 路由文件、测试文件、页面文件超过可维护上限时，优先按能力家族拆分
- 新增能力不得把旧的“超大 `lib.rs` / 巨型 handler / 巨型 test”再做大

## 8. 评估标准

- 一个模块是否只有一个主职责
- 新增能力是否只影响有限边界
- 前端页面是否可通过包结构快速定位业务归属
- 后端代码是否仍保持 `interface -> app -> domain -> infra` 的单向理解路径

## 2026-04-10 S08 Boundary Stability Addendum

- the current `S08` integrated acceptance replay found no new module-boundary drift on the verified commercialization path:
  - admin product surface remains bounded by `sdkwork-api-interface-admin` and admin packages
  - portal product surface remains bounded by `sdkwork-api-interface-portal` and portal packages
  - public commercial surface remains bounded by `sdkwork-api-interface-http`
- the current blocker sits outside module decomposition:
  - release governance evidence production
  - release-window truth replay
  - release-sync truth replay
- no new module split or boundary rewrite is introduced by the current `S08` loop

## 2026-04-11 S08 Boundary Stability Addendum 2

- the fresh governed live replay still introduces no new module-boundary drift:
  - admin commercial-control-plane truth remains bounded by `sdkwork-api-interface-admin`
  - portal and public commercial truth remain bounded by `sdkwork-api-interface-portal` and `sdkwork-api-interface-http`
  - replay-helper compatibility work stays confined to the `artifacts/runtime` evidence tooling path
- the remaining blocker is now even more clearly outside module decomposition:
  - `release-window-snapshot`: `pass`
  - `release-slo-governance`: `pass`
  - `release-sync-audit`: `fail`
- no new module split, dependency inversion, or boundary rewrite is required by the current `S08` loop

## 2026-04-11 S08 Boundary Stability Addendum 3

- the last blocker is now boundary-stable enough to classify as external:
  - current failure facts live in git-root, branch-tracking, remote-governance, and sibling-repository release boundaries
  - they do not indicate a new code-module or interface-boundary defect inside `sdkwork-api-router`
- boundary conclusion:
  - no further module decomposition work is unlocked by the current `release-sync-audit` result
  - next progress depends on release-governance repository hygiene changing, not on rewriting package or crate boundaries
