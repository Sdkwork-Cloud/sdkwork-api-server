# v0.1.34 - Commercial S03 Admin Marketing Coupon Template Revision Governance

- Date: 2026-04-10
- Version: v0.1.34
- Type: patch
- Scope: commercialization `S03` canonical `coupon template` revision governance on admin

## Delivered

- upgraded canonical template governance with additive revision fields:
  - `approval_state`
  - `revision`
  - `root_coupon_template_id`
  - `parent_coupon_template_id`
- added semantic admin revision APIs:
  - `clone`
  - `compare`
  - `submit-for-approval`
  - `approve`
  - `reject`
- made governed rollout approval-aware:
  - template `publish / schedule` now requires approved state for governed revisions
- extended template lifecycle audit with revision evidence:
  - `source_coupon_template_id`
  - `previous_approval_state / resulting_approval_state`
  - `previous_revision / resulting_revision`
- synchronized OpenAPI, admin shared TS types, and admin API client with template revision-governance schemas and methods
- backwrote architecture doc `166` so repo truth now treats template-side `revision / approval / compare / clone` as closed

## Verification

- `cargo test -p sdkwork-api-interface-admin --test marketing_coupon_routes -- --nocapture`
- `cargo test -p sdkwork-api-interface-admin --test openapi_route -- --nocapture`
- `node ./apps/sdkwork-router-admin/tests/admin-marketing-api-surface.test.mjs`

## Known Limits

- campaign still lacks the same `revision / approval / compare / clone` governance layer
- wider `S03` commercialization convergence remains open outside this template revision slice
