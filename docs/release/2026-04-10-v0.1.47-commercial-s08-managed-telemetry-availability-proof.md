# v0.1.47 - Commercial S08 Managed Telemetry Availability Proof

- Date: 2026-04-10
- Version: v0.1.47
- Type: patch
- Scope:
  - `S08` managed telemetry truth refinement
  - release no-go posture precision upgrade

## Delivered

- proved that live managed telemetry is already reachable on the current host:
  - `9980 / 9981 / 9982` health endpoints return `200`
  - authenticated `/metrics` on those managed ports returns real Prometheus text
- captured that live telemetry into an isolated evidence pack under:
  - `artifacts/runtime/dev-s08-telemetry-check/release-input`
- confirmed the governed export half of the release telemetry chain is locally executable:
  - `materialize-release-telemetry-export.mjs` succeeds from live managed `/metrics`
- narrowed the remaining release blocker again:
  - `materialize-release-telemetry-snapshot.mjs` still fails because the governed snapshot is missing `gateway-non-streaming-success-rate`
  - therefore the actionable telemetry gap is truthful `supplemental.targets` for the remaining non-availability SLO targets, not the absence of all local `/metrics`
- corrected `S08` and `166` records so they now distinguish:
  - raw standalone `8080 / 8081 / 8082` being absent
  - managed `9980 / 9981 / 9982` observability already being present

## Verification

- raw-default probe:
  - `Invoke-WebRequest http://127.0.0.1:8080/metrics`
  - `Invoke-WebRequest http://127.0.0.1:8081/metrics`
  - `Invoke-WebRequest http://127.0.0.1:8082/metrics`
- managed-service probe:
  - `Invoke-WebRequest http://127.0.0.1:9980/health`
  - `Invoke-WebRequest http://127.0.0.1:9981/admin/health`
  - `Invoke-WebRequest http://127.0.0.1:9982/portal/health`
  - `Invoke-WebRequest -Headers @{ Authorization = 'Bearer local-dev-metrics-token' } http://127.0.0.1:9980/metrics`
  - `Invoke-WebRequest -Headers @{ Authorization = 'Bearer local-dev-metrics-token' } http://127.0.0.1:9981/metrics`
  - `Invoke-WebRequest -Headers @{ Authorization = 'Bearer local-dev-metrics-token' } http://127.0.0.1:9982/metrics`
- managed startup replay:
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\bin\\start-dev.ps1 -WaitSeconds 240 -GatewayBind 127.0.0.1:19980 -AdminBind 127.0.0.1:19981 -PortalBind 127.0.0.1:19982 -WebBind 127.0.0.1:19983`
  - current failure point: shared `bin/.sdkwork-target-vs2022` lock while removing `portal-api-service.exe`
- governed telemetry chain replay:
  - `node scripts/release/materialize-release-telemetry-export.mjs --gateway-prometheus ... --admin-prometheus ... --portal-prometheus ...`
  - `node scripts/release/materialize-release-telemetry-snapshot.mjs --export artifacts/runtime/dev-s08-telemetry-check/release-input/release-telemetry-export-live.json`

## Release Gate

- result: `no-go`
- current exact posture:
  - `release-window-snapshot`: pass
  - `release-sync-audit`: fail
  - `release-slo-governance`: blocked

## Exact Blocker Truth

- local managed availability telemetry exists and is probeable
- default governed telemetry latest artifacts are still not written into `docs/release`
- the first governed snapshot failure now reproduces as:
  - `release telemetry snapshot is missing target gateway-non-streaming-success-rate`
- remaining truthful gap:
  - `supplemental.targets` for the non-availability SLO targets
  - plus the governed snapshot / SLO latest artifact materialization that depends on them

## Next Loop Focus

- obtain or implement a truthful `supplemental.targets` handoff for the remaining SLO baseline
- write governed latest telemetry snapshot / SLO artifacts only after the full target set is honest
- replay the final `S08` gate together with the still-failing `release-sync-audit`
