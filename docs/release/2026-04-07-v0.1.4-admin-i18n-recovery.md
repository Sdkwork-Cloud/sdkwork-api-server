# v0.1.4 - Admin I18n Recovery and Step 06 Verification

- Date: 2026-04-07
- Type: patch
- Scope: admin control plane, commercial workspace, i18n verification

## Summary

This iteration restores the admin translation load path after multiple corrupted translation source files broke the `zh-CN` catalog and prevented the Step 06 admin verification loop from running against the real workspace.

## Included Changes

1. Updated the admin commercial workbench regression to validate the current split-package implementation.
2. Updated the admin i18n structural gate so it loads the real translation module graph instead of a legacy inline assumption.
3. Rewrote the damaged admin translation source files into stable ASCII-escaped TypeScript objects:
   - `i18nTranslations.ts`
   - `i18nTranslationsCore.ts`
   - `i18nTranslationsCommercial.ts`
   - `i18nTranslationsRouting.ts`
4. Added `i18nTranslationsRecovery.ts` to register 158 missing payment/control-plane keys used by the current admin source tree.
5. Reconnected the recovery catalog into the root `ADMIN_TRANSLATIONS['zh-CN']` graph.

## Verification

### Passed

- `node apps/sdkwork-router-admin/tests/admin-commercial-api-surface.test.mjs`
- `node apps/sdkwork-router-admin/tests/admin-commercial-workbench.test.mjs`
- `node apps/sdkwork-router-admin/tests/admin-i18n-coverage.test.mjs`

### Blocked outside this patch

- `node scripts/check-rust-verification-matrix.mjs --group admin-service`
- `node scripts/check-rust-verification-matrix.mjs --group portal-service`

Shared blocker:

- `crates/sdkwork-api-domain-billing/src/accounts.rs:664`
- `crates/sdkwork-api-domain-billing/src/pricing.rs`

## Known Follow-up

1. Curate Simplified Chinese translations for the recovery slice instead of leaving English fallback values in place.
2. Repair the billing-domain compile failures so the Step 06 admin/portal Rust verification matrix returns to green.
