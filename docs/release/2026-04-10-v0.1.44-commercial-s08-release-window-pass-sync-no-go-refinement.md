# v0.1.44 - Commercial S08 Release Window Pass and Sync No-Go Refinement

- Date: 2026-04-10
- Version: v0.1.44
- Type: patch
- Scope:
  - `S08` release-governance latest-artifact materialization
  - release no-go refinement

## Delivered

- materialized `docs/release/release-window-snapshot-latest.json` from current shell git truth, so the release-window lane no longer depends on Node child-process git replay on this host
- materialized `docs/release/release-sync-audit-latest.json` from current repo and sibling-repo shell truth, so the release-sync lane now reports explicit release failure reasons instead of only `command-exec-blocked`
- updated `S08` step and architecture records so the gate now reads:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`

## Verification

- `git tag --list "release-*" --sort=-creatordate | Select-Object -First 5`
- `git rev-list --count release-2026-03-28-8..HEAD`
- per-repo shell replay:
  - `git status --short --branch`
  - `git rev-parse --show-toplevel`
  - `git rev-parse HEAD`
  - `git remote get-url origin`
- `node scripts/release/materialize-release-window-snapshot.mjs`
- `node scripts/release/materialize-release-sync-audit.mjs`
- `node scripts/release/run-release-governance-checks.mjs --format json`

## Release Gate

- result: `no-go`
- current exact posture:
  - `release-window-snapshot`: pass
  - `release-sync-audit`: fail
  - `release-slo-governance`: blocked

## Known Limits

- `sdkwork-api-router` is dirty and not branch-synced for release
- `sdkwork-core` is not a standalone release root for the governed path and still points at the wrong GitHub remote
- `sdkwork-ui`, `sdkwork-appbase`, and `sdkwork-im-sdk` are all dirty
- governed live telemetry export / snapshot / SLO evidence are still missing

## Next Loop Focus

- clean or isolate the governed release repository set
- repair `sdkwork-core` release boundary and remote alignment
- materialize live telemetry export / snapshot / SLO evidence and rerun the gate
