# v0.1.5 - Step 06 Rust Verification Recovery

- Date: 2026-04-07
- Type: patch
- Scope: Step 06 control-plane Rust verification recovery
- State: not a full Step 06 close; blocker-clearing slice only

## Summary

This iteration repaired the remaining compile-time regressions that blocked the Step 06 control-plane verification chain. The immediate outcome is that both `portal-service` and `admin-service` are green again under the repository-owned split-package verification matrix.

## Decision Ledger

- Primary mode: `blocker-clearing`
- Previous mode: `blocker-clearing`
- Strategy switch: no

### Top Candidate Actions

1. Close the concrete `portal-service` and `admin-service` compile blockers.
   - `Priority Score: 103`
   - chosen because it directly reduced active Step 06 blocker count and produced immediate verification evidence

2. Spend the iteration on warning cleanup first.
   - `Priority Score: 54`
   - rejected because warnings were not the current release gate

3. Write docs before the service groups were green.
   - `Priority Score: 33`
   - rejected because it would document incomplete facts

### Accepted Hypothesis

The failing Step 06 service groups were blocked by split-module visibility regressions, stale manifest edges, and outdated interface-to-app call signatures, not by a new domain-model defect.

### Result

The hypothesis was confirmed. Once those boundary regressions were repaired, the service groups returned to green without requiring behavioral redesign.

## Included Changes

1. Recovered portal crate-root access to split-module state constants and commerce DTOs.
2. Re-exported the app-commerce coupon reservation reclamation helper needed by portal marketing flows.
3. Recovered shared rollout helper visibility between `rollout_execution.rs` and `runtime_reload.rs`.
4. Restored construction visibility for `StandaloneRuntimeSupervision`.
5. Added the missing `sdkwork-api-app-credential` manifest edge to `portal-api-service`.
6. Added the missing `sdkwork-api-app-commerce` manifest edge to `sdkwork-api-interface-admin`.
7. Updated admin commerce handlers to pass the required secret-manager and billing context into the newer refund / reconciliation APIs.

## Verification

### Passed

- `CARGO_TARGET_DIR=target-check-portal-interface-20260407 cargo check -p sdkwork-api-interface-portal`
- `CARGO_TARGET_DIR=target-check-app-runtime-20260407 cargo check -p sdkwork-api-app-runtime`
- `CARGO_TARGET_DIR=target-check-portal-service-20260407 cargo check -p portal-api-service`
- `CARGO_TARGET_DIR=target-check-portal-service-20260407 node scripts/check-rust-verification-matrix.mjs --group portal-service`
- `CARGO_TARGET_DIR=target-check-admin-service-20260407 cargo check -p sdkwork-api-interface-admin`
- `CARGO_TARGET_DIR=target-check-admin-service-20260407 cargo check -j 1 -p admin-api-service`
- `CARGO_TARGET_DIR=target-check-admin-service-20260407 node scripts/check-rust-verification-matrix.mjs --group admin-service`

### Not Claimed In This Slice

- full Step 06 capability closure
- `91` / `95` pass for the whole step
- architecture writeback completion for the entire control-plane milestone
- warning-debt cleanup outside the active blockers

## Impact On Commercial Delivery

- admin and portal service boot chains are no longer blocked at compile time for the current Step 06 branch
- Step 06 can continue from blocker removal into verification hardening and capability closure
- the current commercial-delivery gap is now dominated by remaining capability, testing, and warning-debt work rather than hard Rust build failures

## Remaining Risks

1. Warning debt is still high in the gateway and runtime layers and may hide later regressions if left unattended.
2. `stateless runtime` evidence did not materially improve in this slice.
3. Step 06 still needs business-workspace, E2E, and architecture-capability closure before it can be treated as complete.

## Next Iteration Entry

1. Strengthen tests around the admin commerce refund / reconciliation path that was rewired here.
2. Add focused verification for the portal commerce order-center and reconciliation summary path.
3. Re-score Step 06 against `8.3` / `8.6` after the recovered service groups and only then decide whether to transition into broader Step 06 convergence work.
