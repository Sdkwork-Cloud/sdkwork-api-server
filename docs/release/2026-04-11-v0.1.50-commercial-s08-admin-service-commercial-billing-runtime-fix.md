# v0.1.50 - Commercial S08 Admin Service Commercial Billing Runtime Fix

- Date: 2026-04-11
- Version: v0.1.50
- Type: patch
- Scope:
  - `S08` admin standalone commercial billing runtime repair
  - service-level regression closure for commercial control-plane metrics

## Delivered

- fixed the standalone admin runtime wiring in `services/admin-api-service/src/main.rs`:
  - replaced the old store-only bootstrap path with `build_admin_store_and_commercial_billing_from_config(...)`
  - introduced `build_admin_service_runtime(config)` so startup and tests now share the same runtime assembly path
  - wired `commercial_billing` into both `AdminApiState` and `StandaloneServiceReloadHandles`
- added a stronger source-level regression test in the same file:
  - logs in through `/admin/auth/login`
  - exercises `/admin/billing/account-holds`
  - exercises `/admin/billing/request-settlements`
  - exercises `/admin/billing/pricing-lifecycle/synchronize`
  - confirms `/metrics` increments the `status="200"` counters for all three routes and leaves the matching `status="501"` counters at `0`

## Verification

- targeted service metrics regression:
  - `cargo test -p admin-api-service build_admin_service_runtime_reports_success_metrics_for_commercial_billing_routes -- --nocapture`
- package regression:
  - `cargo test -p admin-api-service -- --nocapture`

## Release Gate

- result: `no-go`

## Exact Blocker Truth

- the repo-local root cause is now fixed and verified from the current source runtime
- this loop did not refresh the governed live latest-artifact chain:
  - `docs/release/release-telemetry-export-latest.json`
  - `docs/release/release-telemetry-snapshot-latest.json`
  - `docs/release/slo-governance-latest.json`
- reason:
  - the current shell policy blocks the child-process/background stack launch pattern needed to replay the source-backed admin/gateway/portal services on this host
  - the last governed live artifacts on disk therefore still reflect the pre-fix runtime posture and cannot be silently promoted to post-fix truth
- `release-sync-audit` also remains independently failing on the last governed replay

## Next Loop Focus

- replay the source-backed stack on a host that allows child-process launch
- re-materialize `release-telemetry-export-latest.json`, `release-telemetry-snapshot-latest.json`, and `slo-governance-latest.json`
- refresh `release-window-snapshot-latest.json` after the next doc backwrite
- recompute final `S08` `go / no-go` from fresh governed live evidence
