# S00 语义冻结、基线审计与阻塞治理 - 2026-04-10

> 注：本文件记录 2026-04-10 重构启动时的基线冻结口径；其中涉及 legacy coupon 的 Current 描述已在同日 `S07` 全量退场中关闭，现仅保留为历史上下文。

## 1. 架构对齐

- 对齐文档：
  - `docs/架构/166-*`
  - `docs/架构/03-*`
  - `docs/架构/133-*`
  - `docs/架构/138-*`
  - `docs/架构/141-*`
  - `docs/step/100-*`
- 对齐能力：
  - 单一术语
  - 单一事实源
  - Current/Target/Deferred 三分
  - legacy freeze
- 本轮必须兑现：
  - 统一 `product / account / coupon / marketing / quote / transaction / entitlement` 术语
  - 冻结阻塞清单、Owner、串并行矩阵
  - 冻结 legacy 禁增列表
- 本轮显式后延：
  - 任何代码层重命名与 schema 迁移
- 完成后应兑现：
  - `166/03/133` 对 `Market`、三层边界、legacy freeze 的统一口径。
- 执行后必须回写：
  - `docs/架构/166-*`
  - `docs/架构/03-*`
  - `docs/review/*`

## 2. Current / Target / Deferred

- Current：
  - 双 coupon 模型并存。
  - `PortalCommerceQuoteKind` 混用产品与交易语义。
  - Admin / Portal 仍暴露 legacy 兼容心智。
  - step 入口尚未形成本轮商业化专项主顺序。
- Target：
  - 本轮所有实施都基于同一术语表与阻塞矩阵推进。
  - 所有后续 step 都有明确 Owner、前置、并行车道、回退口径。
- Deferred：
  - 具体模型与接口改造留给 `S01-S08`。

## 3. 设计

- 先冻结“如何说”，再冻结“如何改”。
- 把阻塞分成四类：
  - 语义阻塞
  - 兼容阻塞
  - 存储阻塞
  - 发布阻塞
- 把 legacy 对象分成两类：
  - 允许短期兼容投影
  - 禁止继续长新语义

## 4. 实施落地规划

- 主写入范围：
  - `docs/架构/166-*`
  - `docs/step/100-110`
  - `docs/review/`
- 关键读取范围：
  - `crates/sdkwork-api-domain-marketing/src/lib.rs`
  - `crates/sdkwork-api-domain-billing/src/pricing.rs`
  - `crates/sdkwork-api-domain-billing/src/accounts.rs`
  - `crates/sdkwork-api-interface-admin/src/marketing.rs`
  - `crates/sdkwork-api-interface-portal/src/marketing_handlers.rs`
  - `apps/sdkwork-router-portal/packages/sdkwork-router-portal-types/src/index.ts`
- 实施顺序：
  1. 冻结术语与能力边界。
  2. 冻结 legacy 禁增列表。
  3. 冻结 step 依赖与并行窗口。
  4. 冻结每步的完成定义和证据口径。

## 5. 串行与并行

- Step 属性：串行收口。
- 允许多子 agent 并行的仅限只读盘点车道。

| 车道 | 子域 | 写集 | 前置 | 交付物 |
| --- | --- | --- | --- | --- |
| `L1` | product/pricing 盘点 | 无 | 无 | 文件清单、阻塞点 |
| `L2` | account/billing 盘点 | 无 | 无 | 文件清单、阻塞点 |
| `L3` | coupon/marketing 盘点 | 无 | 无 | 文件清单、阻塞点 |
| `L4` | admin/portal 盘点 | 无 | 无 | 文件清单、阻塞点 |
| `L5` | Owner 收口 | `docs/step/100-110` | `L1-L4` | 统一 step 体系 |

## 6. 测试计划

- 文档与事实核验：
  - `rg -n "legacy coupon compatibility|legacy coupon projection|coupon_redemption" crates apps docs -g "*.rs" -g "*.ts" -g "*.tsx" -g "*.md"`
- 结构核验：
  - 确认 `100-110` 已全部落地并互相链接。
- 结果不依赖编译；本步是治理冻结步，不做代码改造。

## 7. 结果验证

- 主证据：
  - `docs/step/100-*`
  - `docs/step/102-*`
- 辅证据：
  - `docs/架构/166-*`
  - 关键 `rg` 盘点结果
- 架构回写：
  - `166`、`03` 的术语、边界、legacy freeze 状态同步
- 通过条件：
  - 无术语悬空项
  - 无未归属阻塞项
  - 无未定义 Owner 的并行车道

## 8. 检查点

- `CPC00-1`：术语表、主模型边界、legacy freeze 规则冻结。
- `CPC00-2`：`S00-S08` 串行主脊柱与并行窗口冻结。
- `CPC00-3`：每步的完成定义、证据、回退口径冻结。

## 9. 风险与回退

- 风险：
  - 语义冻结不彻底，后续 step 反复改口径。
  - 共享文件 Owner 不清导致并行冲突。
- 回退触发条件：
  - 任一关键术语在后续 step 中出现双重定义。
- 回退动作：
  - 停止进入下一步。
  - 回写 `S00` 与 `100`，重新冻结术语和 owner。

## 10. 完成定义

- 本轮商业化专项的统一口径已固定。
- 每个后续 step 都能直接引用本步的术语、阻塞和并行规则。
- `166` 对应术语与边界不再停留在 Target 叙述。

## 11. 下一步准入

- 准入结论：`go`
- 允许启动的下一步：`S01`
- 仍需阻塞的项：
  - 任何不经过 `S01-S08` 的直接代码发散式重构
