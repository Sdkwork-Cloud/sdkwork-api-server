# S08 集成验收、发布门禁与持续迭代 - 2026-04-10

> 注：本文件是商业化主线的总验收门禁说明；legacy coupon 相关检查项现在应只作为“禁回归”验证，不再表示存在兼容保留目标。

## 1. 架构对齐

- 对齐文档：
  - `docs/架构/166-*`
  - `docs/架构/133-*`
  - `docs/架构/138-*`
  - `docs/架构/141-*`
  - `docs/架构/137-*`
  - `docs/step/100-*`
- 对齐能力：
  - 端到端商业化验收
  - 发布门禁
  - 架构回写
  - 持续迭代入口
- 本轮必须兑现：
  - `S00-S07` 的成果被统一拉通验证，而不是各自局部通过。
  - 发布、回滚、证据、架构回写形成同一闭环。
  - 下一波迭代 backlog 与 deferred 清单冻结。
- 本轮显式后延：
  - 新一轮功能扩展
  - 新子域立项
- 完成后应兑现：
  - `166` 中分阶段实施路线、验收标准与最终结论对应的整体验收能力。
- 执行后必须回写：
  - `docs/架构/166-*`
  - `docs/架构/133-*`
  - `docs/架构/03-*`
  - `docs/review/*`
  - `docs/release/*`

## 2. Current / Target / Deferred

- Current：
  - `docs/架构/166-*` 已给出目标态，`docs/step/100-109` 定义了改造路径。
  - 当前缺少面向商业化专项的统一 evidence matrix、release gate 与 final sign-off。
  - 如果没有总验收，局部通过会重新演变成新的语义漂移。
- Target：
  - 形成“架构 -> 代码 -> 接口 -> UI -> 存储 -> 发布 -> 回滚”的单一验收包。
  - 所有 `CPC` 检查点都有证据链接、风险结论与下一步决议。
  - 文档把 `Current / Target / Deferred` 更新到最新状态，后续迭代从明确 backlog 继续。
- Deferred：
  - 新一波合作伙伴分发、复杂促销叠加、离线分析平台

## 3. 设计

- `S08` 不再新增业务语义，只做验收、门禁、回写、发布与后续计划。
- 验收必须分五层：
  - backend domain / app
  - interface / openapi / sdk contract
  - admin / portal UX
  - storage / migration / rollback
  - docs / release / review evidence
- 若任一上游 `CPC` 未闭环，`S08` 只能输出阻塞结论，不能输出“已完成”。

## 4. 实施落地规划

- 主写入范围：
  - `docs/review/*`
  - `docs/架构/166-*`
  - `docs/架构/133-*`
  - `docs/架构/03-*`
  - `docs/step/100-*`
  - `docs/release/*`
- 关键读取范围：
  - `crates/sdkwork-api-app-commerce/tests/*`
  - `crates/sdkwork-api-interface-admin/tests/*`
  - `crates/sdkwork-api-interface-portal/tests/*`
  - `apps/sdkwork-router-admin/tests/*`
  - `apps/sdkwork-router-portal/tests/*`
  - `data/*`
- 实施顺序：
  1. 汇总 `S00-S07` 检查点状态与证据缺口。
  2. 运行后端、前端、OpenAPI、迁移、回滚验证矩阵。
  3. 生成 `docs/review/*` 证据、回写 `docs/架构/*` 当前状态。
  4. 输出发布门禁结论、回滚包与下一波 backlog。

## 5. 串行与并行

- Step 属性：验证与文档车道可并行；最终 gate 与 sign-off 必须串行。
- 共享文件 Owner：
  - `docs/架构/166-*`
  - `docs/review/*`
  - `docs/release/*`

| 车道 | 子域 | 写集 | 前置 | 交付物 |
| --- | --- | --- | --- | --- |
| `L1` | backend regression | Rust tests、migration replay | 无 | 后端通过证据 |
| `L2` | portal/admin contract + UX | TS tests、typecheck、openapi diff | 无 | 前端与契约通过证据 |
| `L3` | release / rollback drill | release checklist、rollback note | `L1-L2` | 发布门禁结论 |
| `L4` | docs backwrite | `docs/review/*`、`docs/架构/*`、`docs/release/*` | `L1-L3` | 回写与 backlog |
| `LO` | owner sign-off | 总结论 | `L1-L4` | 最终 go / no-go |

## 6. 测试计划

- Rust：
  - `cargo test -p sdkwork-api-app-billing -- --nocapture`
  - `cargo test -p sdkwork-api-app-commerce -- --nocapture`
  - `cargo test -p sdkwork-api-interface-admin -- --nocapture`
  - `cargo test -p sdkwork-api-interface-portal -- --nocapture`
  - `cargo test -p sdkwork-api-storage-sqlite -- --nocapture`
  - `cargo test -p sdkwork-api-storage-postgres -- --nocapture`
- TypeScript / 前端：
  - `pnpm --dir apps/sdkwork-router-admin typecheck`
  - `pnpm --dir apps/sdkwork-router-portal typecheck`
  - `node --test apps/sdkwork-router-admin/tests/admin-crud-ux.test.mjs`
  - `node --test apps/sdkwork-router-admin/tests/admin-product-experience.test.mjs`
  - `node --test apps/sdkwork-router-portal/tests/portal-commercial-api-surface.test.mjs`
  - `node --test apps/sdkwork-router-portal/tests/portal-marketing-api-surface.test.mjs`
- OpenAPI / 文档 / 退场核验：
  - `cargo test -p sdkwork-api-interface-admin openapi_route -- --nocapture`
  - `cargo test -p sdkwork-api-interface-portal openapi_route -- --nocapture`
  - `rg -n "legacy coupon compatibility|legacy coupon crate|legacy admin coupon entry" crates apps docs -g "*.rs" -g "*.ts" -g "*.tsx" -g "*.md"`

## 7. 结果验证

- 主证据：
  - `product / account / coupon-first marketing` 三层都能用同一条业务叙事解释。
  - Admin、Portal、Public API、storage、runtime 的语义一致。
  - release gate、rollback note、review evidence 齐全。
- 辅证据：
  - `docs/架构/166-*`、`docs/架构/133-*`、`docs/架构/03-*` 已同步最新 Current / Target / Deferred。
- 架构回写：
  - `166/133/03` 状态、`docs/review/*` 证据、`docs/release/*` 结论全部闭环
- 通过条件：
  - 所有 `CPC00-CPC07` 已闭环或显式降级并获得 go/no-go 结论。

## 8. 检查点

- `CPC08-1`：统一 evidence matrix 完成，`S00-S07` 检查点全部挂证据。
- `CPC08-2`：发布门禁、迁移回放、回滚演练通过，形成最终 go / no-go。
- `CPC08-3`：架构回写、release 文档与下一波 backlog 冻结完成。

## 9. 风险与回退

- 风险：
  - 局部成功被误判为整体完成。
  - release gate 漏掉 contract drift、migration rollback 或 UI 断面。
  - 文档不回写会导致下轮迭代继续基于旧口径。
- 灰度：
  - 先形成 pre-release evidence 包，再决定是否进入 release candidate。
- 回退触发条件：
  - 任一关键 regression、contract drift、migration rollback 演练失败。
- 回退动作：
  - 直接判定 `no-go`。
  - 回到对应上游 step 补闭环，不允许带病发布。

## 10. 完成定义

- 本轮商业化专项已从“设计与分步改造计划”收口为“可发布、可回滚、可继续迭代”的专业化能力版本。
- `166` 中本轮可兑现能力都已有最终证据、发布结论与 Deferred 冻结结果。

## 11. 下一步准入

- 准入结论：`go` 或 `no-go`
- 允许启动的下一步：
  - 下一波商业化 backlog
  - `docs/架构/*` 中 Deferred 的后续专项
- 仍需阻塞的项：
  - 未经 `S08` gate 的对外完成宣称或发布动作

## 2026-04-10 S08 Loop Addendum 1

- integrated acceptance now has fresh evidence across:
  - admin control-plane Node source-contract and UX suites
  - portal commercial and marketing Node source-contract suites
  - admin backend legacy coupon route-removal proof
  - gateway public `market / marketing / commercial` runtime and OpenAPI proof
- current release gate result is `no-go`, not because `S03/S06/S07` regressed, but because governed live release-truth lanes remain blocked:
  - `release-slo-governance`: `telemetry-input-missing`
  - `release-window-snapshot`: `command-exec-blocked`
  - `release-sync-audit`: `command-exec-blocked`
- `CPC08-1` is materially closed for the verified admin / portal / public evidence chain and documentation backwrite
- `CPC08-2` remains blocked until governed telemetry, release-window, and release-sync inputs are materialized on a host that can prove release Git truth
- current next `S08` upgrade focus narrows to:
  - governed release telemetry export / snapshot / SLO evidence production
  - governed release-window and release-sync artifact materialization
  - final sign-off replay on a host that can verify remote refs and sibling repository alignment

## 2026-04-10 S08 Loop Addendum 2

- governed latest-artifact replay now closes the `release-window-snapshot` lane on the current host:
  - `docs/release/release-window-snapshot-latest.json`
  - source kind: `shell-git-governed`
- `release-sync-audit` is no longer only a host-exec block; it now replays as an evidence-backed release `fail` with concrete reasons:
  - root repo: `dirty-working-tree`, `branch-not-synced`, `remote-unverifiable`
  - `sdkwork-core`: `not-standalone-root`, `dirty-working-tree`, `remote-url-mismatch`, `remote-unverifiable`
  - `sdkwork-ui`, `sdkwork-appbase`, `sdkwork-im-sdk`: `dirty-working-tree`, `remote-unverifiable`
- current release-governance structure is therefore:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`
- `S08` remains `no-go`, but the remaining gate is now more truthful and actionable than the earlier all-blocked posture

## 2026-04-10 S08 Loop Addendum 3

- release-window governance replay needed one more freshness repair after subsequent `S08` doc/release backwrite changed workspace truth
- root cause is now explicit:
  - `compute-release-window-snapshot.mjs` resolves the default latest artifact before any live Git collection
  - this means `docs/release/release-window-snapshot-latest.json` must be re-materialized whenever later loop backwrite changes the working tree count
- governed release-window truth has been refreshed again from current shell git evidence:
  - `latestReleaseTag = release-2026-03-28-8`
  - `commitsSinceLatestRelease = 22`
  - `workingTreeEntryCount = 518`
- the gate structure itself does not change:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`
- `S08` therefore remains `no-go` for the same final reasons:
  - governed sibling-repo release hygiene is still negative
  - governed live telemetry input is still missing

## 2026-04-10 S08 Loop Addendum 4

- the telemetry blocker is now narrowed from a generic `missing telemetry` label into a reproducible external-input gap:
  - current shell telemetry env var count: `0`
  - current latest governed telemetry artifacts:
    - `docs/release/release-telemetry-export-latest.json`: absent
    - `docs/release/release-telemetry-snapshot-latest.json`: absent
    - `docs/release/slo-governance-latest.json`: absent
  - documented local metrics sources on `127.0.0.1:8080/8081/8082` are unreachable on the current host
- direct script replay confirms the same truth:
  - `materialize-release-telemetry-export.mjs`: missing telemetry export input
  - `materialize-release-telemetry-snapshot.mjs`: missing telemetry input
  - `slo-governance.mjs --format json`: `evidence-missing`
- this means the remaining `release-slo-governance` gap is not a repo-local code hole:
  - the current repo already owns the materializers and contract chain
  - but it still lacks a real hosted control-plane telemetry handoff and truthful `supplemental.targets`
- `S08` remains `no-go` with the same gate structure:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`

## 2026-04-10 S08 Loop Addendum 5

- the prior `8080 / 8081 / 8082` probe result is still true, but it is now understood as a raw-standalone boundary rather than a proof that local observability is absent:
  - those raw standalone defaults remain unreachable on the current host
  - managed local services are already live on `127.0.0.1:9980 / 9981 / 9982`
- current managed-service truth is materially stronger:
  - `http://127.0.0.1:9980/health`: `200`
  - `http://127.0.0.1:9981/admin/health`: `200`
  - `http://127.0.0.1:9982/portal/health`: `200`
  - authenticated `http://127.0.0.1:9980/metrics`, `9981/metrics`, and `9982/metrics` all return Prometheus text with `Bearer local-dev-metrics-token`
- that live managed telemetry is enough to close the first half of the governed release-telemetry chain:
  - `materialize-release-telemetry-export.mjs` succeeds from live managed `/metrics`
  - isolated evidence path: `artifacts/runtime/dev-s08-telemetry-check/release-input/release-telemetry-export-live.json`
- the remaining blocker is now narrower and more precise than the earlier generic external-input diagnosis:
  - `materialize-release-telemetry-snapshot.mjs --export ...release-telemetry-export-live.json` fails with `release telemetry snapshot is missing target gateway-non-streaming-success-rate`
  - this proves the still-missing truth is not managed availability telemetry, but truthful coverage for the remaining non-availability SLO targets
  - in current repo terms, that means truthful `supplemental.targets` are still absent
- an attempted isolated `start-dev.ps1` replay also clarified the local runtime boundary:
  - warm-up failed on Windows because the shared `bin/.sdkwork-target-vs2022` output was locked while removing `portal-api-service.exe`
  - that failure is consistent with the host already running live `admin-api-service`, `gateway-service`, and `portal-api-service` processes, not with managed runtime impossibility
- the gate posture therefore stays:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`
- but the blocker wording is now more exact:
  - local managed availability metrics exist
  - default governed latest telemetry snapshot / SLO evidence still do not
  - the actionable telemetry gap is truthful `supplemental.targets`, not the absence of any local `/metrics`

## 2026-04-11 S08 Loop Addendum 6

- a truthful local derived-target probe now closes six non-availability SLO targets that were previously only described as missing:
  - `routing-simulation-p95-latency`: `20/20` successful simulations, `p95Ms = 30`
  - `api-key-issuance-success-rate`: `3/3`
  - `runtime-rollout-success-rate`: `1/1`, rollout reached `succeeded`
  - `gateway-non-streaming-success-rate`: `3/3`
  - `gateway-streaming-completion-success-rate`: `3/3`, every stream terminated with `[DONE]`
  - `billing-event-write-success-rate`: `6/6`
- the governed telemetry chain is now narrowed one step further:
  - `materialize-release-telemetry-export.mjs` succeeds against live managed `/metrics` plus `supplemental-targets-local-probe.json`
  - `materialize-release-telemetry-snapshot.mjs --export ...release-telemetry-export-local-probe.json` now fails at `gateway-fallback-success-rate`
  - this is a real advance from the earlier first failure point `gateway-non-streaming-success-rate`
- the remaining `release-slo-governance` blockers are now smaller and more concrete:
  - no truthful fallback execution samples and no `sdkwork_gateway_failovers_total` samples were emitted during the live gateway probe
  - no truthful provider-timeout or upstream retry/failure samples were emitted for the timeout-budget target
  - canonical commercial control-plane routes for `account-holds`, `request-settlements`, and `pricing-lifecycle/synchronize` return `501` on the current runtime
  - the portal canonical billing account route still returns `404` because the workspace commercial account is not provisioned
- correlated live evidence also proves the probe is not synthetic:
  - gateway requests created usage records
  - gateway requests created billing events
  - gateway routing decision logs recorded `decision_source = gateway`
  - billing events and decision logs carried `fallback_reason = policy_candidate_unavailable`, but gateway `/metrics` still emitted no provider-level failover / retry / execution-context samples
- the release gate posture remains unchanged even though the truthful blocker is narrower:
  - `release-window-snapshot`: `pass`
  - `release-sync-audit`: `fail`
  - `release-slo-governance`: `blocked`
- `S08` therefore remains `no-go`:
  - internal commercialization surfaces remain converged
  - release truth is still blocked by incomplete fallback / timeout telemetry evidence and by independent release-sync hygiene failure

## 2026-04-11 S08 Loop Addendum 7

- the governed release telemetry lane is now fully materialized on the current host and no longer blocked by missing input:
  - `docs/release/release-telemetry-export-latest.json`
  - `docs/release/release-telemetry-snapshot-latest.json`
  - `docs/release/slo-governance-latest.json`
- the last repo-local materialization blocker was a parser bug, not a telemetry absence:
  - `materialize-release-telemetry-snapshot.mjs` previously rejected valid live Prometheus samples when a route label contained template braces such as `/admin/runtime-config/rollouts/{rollout_id}`
  - that parser path is now covered by a targeted regression test and accepts current live admin metrics truthfully
- a new live local stateful-provider governance replay now closes the remaining gateway telemetry targets:
  - `gateway-fallback-success-rate`: `1/1`
  - `gateway-provider-timeout-budget`: `0/1`
  - failover evidence chain:
    - `RoutingDecisionLog` with `fallback_reason = gateway_execution_failover`
    - `ProviderHealthSnapshot` for failed primary and healthy backup
    - `sdkwork_gateway_failovers_total` plus upstream success/failure counters
- `release-slo-governance` therefore shifts from `blocked` to quantitative `fail`
- current failing governed targets are explicit:
  - `admin-api-availability`
  - `account-hold-creation-success-rate`
  - `request-settlement-finalize-success-rate`
  - `pricing-lifecycle-synchronize-success-rate`
- the admin availability miss is now explainable from live metrics instead of missing evidence:
  - the current admin `/metrics` totals include accumulated `501` responses from the unavailable commercial billing routes, which pushes `admin-api-availability` below objective
- `S08` remains `no-go`, but for a stronger and more actionable reason than before:
  - release truth is no longer missing
  - release truth is now evidence-complete enough to fail quantitatively on admin/commercial-control-plane targets while `release-sync-audit` still fails independently

## 2026-04-11 S08 Loop Addendum 8

- the repo-local admin commercial-control-plane defect is now closed in current source:
  - `services/admin-api-service/src/main.rs` now builds commercial billing through `build_admin_store_and_commercial_billing_from_config(...)`
  - the standalone admin runtime now threads that kernel into both `AdminApiState` and `StandaloneServiceReloadHandles`
- service-level runtime evidence is now stronger than the earlier route-existence checks:
  - a new regression test logs in through `/admin/auth/login`
  - it then exercises:
    - `GET /admin/billing/account-holds`
    - `GET /admin/billing/request-settlements`
    - `POST /admin/billing/pricing-lifecycle/synchronize`
  - it verifies `/metrics` records those routes on `status="200"` and leaves the matching `status="501"` counters at `0`
- this is a real upgrade over Addendum 7:
  - the old unavailable-commercial-billing path is no longer reproducible from the current source runtime
  - the remaining gap is now host-level release evidence freshness, not unresolved repo-local runtime wiring
- `S08` still remains `no-go` in this loop:
  - the current shell policy blocks the child-process/background launch pattern needed to replay the full source-backed stack on this host
  - the governed latest artifacts under `docs/release/*latest.json` therefore still reflect the pre-fix live replay and cannot be silently reclassified as post-fix truth
- next gate:
  - rerun the source-backed admin/gateway/portal stack on a host that allows child-process launch
  - re-materialize `release-telemetry-export-latest.json`, `release-telemetry-snapshot-latest.json`, and `slo-governance-latest.json`
  - refresh `release-window-snapshot-latest.json` after the next backwrite and then recompute final `S08` `go / no-go`

## 2026-04-11 S08 Loop Addendum 9

- fresh governed live replay is now closed on the current host after fixing the replay-helper compatibility edge:
  - `POST /admin/auth/login`: `200`
  - `GET /admin/billing/summary`: `200`
  - `GET /admin/billing/account-holds`: `200`
  - `GET /admin/billing/request-settlements`: `200`
  - `POST /admin/billing/pricing-lifecycle/synchronize`: `200`
  - fresh pass-replay evidence is now stored under `artifacts/runtime/dev-s08-telemetry-check/release-input/*local-governance-pass-replay*`
- the governed telemetry chain is no longer failing on commercialization/runtime truth:
  - isolated replay artifacts pass the full `release-slo-governance` baseline
  - latest governed artifacts under `docs/release/release-telemetry-export-latest.json`, `release-telemetry-snapshot-latest.json`, and `slo-governance-latest.json` now replay as `14 / 14` targets passing
- the overall release gate is therefore narrowed to one remaining lane:
  - `release-window-snapshot`: `pass`
  - `release-slo-governance`: `pass`
  - `release-sync-audit`: `fail`
- the release-governance latest artifacts are now refreshed from this same loop truth:
  - `release-window-snapshot-latest.json` records `latestReleaseTag = release-2026-03-28-8`, `commitsSinceLatestRelease = 22`, `workingTreeEntryCount = 527`
  - `release-sync-audit-latest.json` replays the same explicit cross-repo hygiene failure reasons from current shell git truth
- `S08` still remains `no-go`, but the blocker is now fully explicit and externalized away from product/runtime closure:
  - `sdkwork-api-router` remains dirty, branch-unsynced, and remote-unverifiable
  - `sdkwork-core` remains non-standalone-root, dirty, remote-url-mismatched, and remote-unverifiable
  - `sdkwork-ui`, `sdkwork-appbase`, and `sdkwork-im-sdk` remain dirty and remote-unverifiable
- next gate:
  - refresh `release-window-snapshot-latest.json` after any further backwrite
  - refresh `release-sync-audit-latest.json` whenever governed repo hygiene truth changes
  - keep `S08` at `no-go` until the sync-audit blocker is cleared or explicitly accepted as an external release blocker

## 2026-04-11 S08 Loop Addendum 10

- the remaining `S08` blocker now satisfies the Step prompt stop condition for an explicit external blocker:
  - evidence is current and concrete
  - impact is explicit
  - unlock conditions are explicit
  - next-loop entry is explicit
- refined release-sync root-cause evidence:
  - current `sdkwork-api-router` branch is `wip/root-main-preserve-2026-04-09`
  - current `sdkwork-api-router` branch has no upstream configured
  - current `sdkwork-api-router` working tree is dirty
  - current `sdkwork-core` top-level git root is `D:/javasource/spring-ai-plus`, not the governed standalone root
  - current `sdkwork-core` origin URL points at `sdkwork-backend-react-web`, not `sdkwork-core`
- blocker impact:
  - `release-window-snapshot = pass`
  - `release-slo-governance = pass`
  - `release-sync-audit = fail`
  - therefore `release_closure = yes` still cannot be claimed
- unlock conditions:
  - clean and govern the current root repo release branch/tag truth
  - audit `sdkwork-core` from its standalone root with the governed origin URL
  - make governed sibling repositories clean and remotely verifiable
  - rerun `materialize-release-sync-audit.mjs` and `run-release-governance-checks.mjs --format json`
- next-loop entry:
  - only re-enter active `S08` execution when release-sync hygiene changes or when a human release owner formally accepts this blocker as an external release constraint
